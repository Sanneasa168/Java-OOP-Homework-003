public interface MathSolver {

    // Method sum cannot provide body
    int sum(int...numbers);

    // Method subtract  cannot provide body
    int subtract(int a,int b);
    // Method multipy cannot provide body
    int  multiply(int a,int b);
}
